/** Default SVG size passed to `CycleRing`. */
export const CYCLE_RING_SIZE = 232;

/** White outer disc: concentric barrier around the ring (equal inset on all sides). */
export const CYCLE_RING_OUTER_DISC = CYCLE_RING_SIZE + 16;
